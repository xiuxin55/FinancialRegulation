<Window x:Class="BaseManage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ad="clr-namespace:AvalonDock;assembly=AvalonDock"
        xmlns:local="clr-namespace:BaseManage"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <!--<Button Content="Show" Height="23" HorizontalAlignment="Left" Margin="0,32,0,0" Name="button1" VerticalAlignment="Top" Width="119" Click="button1_Click" />
        <Button Content="ShowAsDocument" Height="23" HorizontalAlignment="Left" Margin="0,101,0,0" Name="button2" VerticalAlignment="Top" Width="119" Click="button2_Click" />
        <Button Content="Show" Height="23" HorizontalAlignment="Left" Margin="0,172,0,0" Name="button3" VerticalAlignment="Top" Width="119" Click="button3_Click" />-->
        <ad:DockingManager>
            <ad:DockingManager.LayoutTransform>
                <ScaleTransform ScaleX="{Binding Value, ElementName=zoomSlider}" ScaleY="{Binding Value, ElementName=zoomSlider}"/>
            </ad:DockingManager.LayoutTransform>
            <ad:ResizingPanel Orientation="Horizontal" >
                <ad:DocumentPane>
                    <local:MenuManageWindow></local:MenuManageWindow>
                </ad:DocumentPane>
            </ad:ResizingPanel>
        </ad:DockingManager>
    </Grid>
</Window>
